// use super::super::objects::Task;
// use crate::prelude::ApplicationState;
// use anyhow::Result;
// use uuid::Uuid;

// pub fn add_todo_item(state: &mut ApplicationState, item: Task) -> Result<()> {
//     state.tasks.insert(Uuid::new_v4().to_string(), item);
//     Ok(())
// }
